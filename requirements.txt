# Python Library Version Requirements
# There is a newer version of numpy (1.19.4), but there are issues with it that are expected to be patched in early 2021

matplotlib==3.3.3
numpy==1.19.3
pandas==1.1.5
scipy==1.5.4
